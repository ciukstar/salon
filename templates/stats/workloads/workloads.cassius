
main
  ##{toolbar}
    padding: 1rem

##{dlgTimeFrame}
  ##{formTimeFrame}
    display: flex
    flex-direction: column
    gap: 1rem
    .form-field
      display: flex
      flex-direction: column
  .mdc-dialog__actions
    gap: 0.5rem