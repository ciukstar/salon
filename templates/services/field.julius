
Array.from(
  document.querySelectorAll('span.currency')
).forEach(function (x) {
  x.textContent = Number(x.textContent).toLocaleString(
    navigator.language,
    { minimumFractionDigits: 0,
      maximumFractionDigits: 2
    }
  );
});
