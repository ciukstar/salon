
document.getElementById('appointmentFullLongTime').textContent = new Date(
  document.getElementById('appointmentTime').getAttribute('datetime')
).toLocaleDateString(
  navigator.language,
  { weekday: 'long',
    year: 'numeric',
    month: 'long',
    day: 'numeric',
    hour: "numeric",
    minute: "numeric"
  }
);
